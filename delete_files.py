# import os
#import shutil
#os.unlink() deletes a single file
#os.rmdir() delete an empty folder
#shutil.rmtree() deletes a folder and its entire content
# Deleting can be dangerous so do a dry run first
#send2trash.send2trash() will send a file or forder to the recycling bin